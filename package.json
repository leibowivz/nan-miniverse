{
  "name": "miniverse-public",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "start": "node server.js",
    "dev": "concurrently -n vite,server -c cyan,magenta \"vite\" \"node server.js\""
  },
  "dependencies": {
    "@miniverse/core": "^0.2.0",
    "@miniverse/server": "^0.2.0"
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "vite": "^5.4.0"
  }
}
